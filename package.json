{
  "private": true,
  "name": "davinci-design",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clear-all": "lerna clean && yarn workspaces run clean",
    "build": "lerna run --stream --sort build",
    "start": "lerna run --stream --sort start",
    "test": "lerna run --stream --sort test",
    "new": "plop",
    "publish": "lerna publish",
    "start:cli": "react-custom-service"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
