import React from "react";
import "./Button.less";
const style = {
  background: "#00c",
  color: "#fff",
  padding: 12,
};

const Button = () => <button style={style}>App Base Button</button>;

export default Button;
